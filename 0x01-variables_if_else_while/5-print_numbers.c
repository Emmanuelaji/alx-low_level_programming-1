#include <stdlib.h>
#include <time.h>
#include <stdio,h>
/**
 * main - Entry point
 * Return:Always 0(Success)
 */
int main(void)
{
int n;
for (n = o; n < 10; n++)
{
printf("%d\n", n);
}
return (0);
}
